const data = {
  intro: `A "living Design System" is a web document that shows all components interface elements(UI).It informs about the typography, the colors, the grid, the iconography, the interactions ...`,
};

module.exports = data;
