import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { ArgsTable } from '@storybook/addon-docs';
import Infos from './Infos';

<Meta title="Structure/Header/Infos" component={Infos} argTypes={{}} />

# Infos

export const Template = (args) => (
  <Infos {...args} />
);

The header package exports an `Infos` in order to display a list of descriptions.  
To do so, have an `infos` property which is an array of object that will be shown one after another into the header. That array has a particular type and contains the following properties :
- an `id`, which is optionnal
- a `word`, which will specify the term
- a `definition`, which will specify the element description

Example : 

```javascript
const infos = [
  {
    word: 'Client :',
    definition: '0123456789 - NOM',
  },
  {
    word: 'Portefeuille :',
    definition: '000123456789',
  },
  {
    word: 'Status :',
    definition: (
      <span style={{ textDecoration: 'underline' }}>Affaire nouvelle</span>
    ),
  },
];
```

Don't forget to load the css file of the component.

```javascript
import '@axa-fr/react-toolkit-layout-header/dist/Infos/af-infos.css';
```

export const infos = [
  {
    word: 'Client :',
    definition: '0123456789 - NOM',
  },
  {
    word: 'Portefeuille :',
    definition: '000123456789',
  },
  {
    word: 'Status :',
    definition: (
      <span style={{ textDecoration: 'underline' }}>Affaire nouvelle</span>
    ),
  },
];

<Canvas>
  <Story
    name="Default"
    args={{
      infos
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
